// NOTE:  Final calculation function.
//        Takes in an object with the data needed and returns
//        and object with the results.

export default function calculationReport(data: object): boolean {
  console.log(data);
  
  return true;
}